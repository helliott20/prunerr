interface:
  display_name: "Prunerr"
  short_description: "Repo-specific patterns and workflows for prunerr"
  default_prompt: "Use the prunerr repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true