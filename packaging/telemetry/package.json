{
  "name": "prunerr-telemetry",
  "version": "1.0.0",
  "description": "Cloudflare Worker that counts live Prunerr installs",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "license": "MIT"
}
